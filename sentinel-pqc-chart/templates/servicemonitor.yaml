apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sentinel-pqc-shadow-monitor
  namespace: monitoring
  labels:
    release: monitoring-stack
spec:
  selector:
    matchLabels:
      app: sentinel-pqc-metrics
  endpoints:
  - port: metrics
    interval: 10s
